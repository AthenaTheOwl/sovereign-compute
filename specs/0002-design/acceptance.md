# acceptance - 0002-design

Accepted when:

- `python -m sovereign_compute scorecard --quarter 2026q2` writes both artifacts.
- `python -m sovereign_compute validate` passes.
- `python scripts/validate_schemas.py` passes.
- `python eval/sanity_bounds.py` passes.
- `python -m pytest tests/ -q` passes.

